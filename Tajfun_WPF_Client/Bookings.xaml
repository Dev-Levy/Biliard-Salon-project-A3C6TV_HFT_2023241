<Window
    x:Class="Tajfun_WPF_Client.Bookings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Tajfun_WPF_Client"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:Tajfun_WPF_Client.ViewModels"
    Title="Bookings"
    Width="800"
    Height="450"
    Background="#8ecae6"
    mc:Ignorable="d">
    <Window.DataContext>
        <views:BookingViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ListBox
            Grid.Column="0"
            HorizontalContentAlignment="Stretch"
            Background="Transparent"
            ItemsSource="{Binding Bookings}"
            ScrollViewer.HorizontalScrollBarVisibility="Hidden"
            ScrollViewer.VerticalScrollBarVisibility="Hidden"
            SelectedItem="{Binding SelectedBooking}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.RowSpan="2"
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Text="{Binding Customer.Name}" />
                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="1"
                                Padding="5"
                                Text="{Binding StartDate, StringFormat='yyyy/MM/dd HH:mm'}" />
                            <TextBlock
                                Grid.Row="1"
                                Grid.Column="1"
                                Padding="5"
                                Text="{Binding EndDate, StringFormat='yyyy/MM/dd HH:mm'}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Column="1" DataContext="{Binding Se}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="2*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="2*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="2*" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Margin="5"
                VerticalAlignment="Center"
                Text="Name" />
            <TextBox
                Grid.Row="1"
                Margin="5"
                VerticalContentAlignment="Center"
                Text="{Binding SelectedBooking.Customer.Name}" />

            <TextBlock
                Grid.Row="2"
                Margin="5"
                VerticalAlignment="Center"
                Text="Starts" />
            <DatePicker
                Grid.Row="3"
                Margin="5"
                VerticalContentAlignment="Center"
                Text="{Binding SelectedBooking.StartDate}" />

            <TextBlock
                Grid.Row="4"
                Margin="5"
                VerticalAlignment="Center"
                Text="Ends" />
            <DatePickerTextBox
                Grid.Row="5"
                Margin="5"
                VerticalContentAlignment="Center"
                Text="{Binding SelectedBooking.EndDate}" />
        </Grid>
    </Grid>
</Window>
